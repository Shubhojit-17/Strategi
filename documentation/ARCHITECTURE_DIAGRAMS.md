# 🎨 Architecture Diagrams

## System Architecture (High-Level)

```
┌─────────────────────────────────────────────────────────────┐
│                      USER INTERFACE                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Crossmint  │  │   wagmi +    │  │  Provenance  │     │
│  │    Login     │  │  RainbowKit  │  │    Viewer    │     │
│  │ (Email/Social)│  │  (Web3 UI)  │  │  (Receipts)  │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                  │               │
└─────────┼─────────────────┼──────────────────┼───────────────┘
          │                 │                  │
          ▼                 ▼                  ▼
┌─────────────────────────────────────────────────────────────┐
│                     FRONTEND LAYER                           │
│                    (Next.js + TypeScript)                    │
│  ┌────────────────────────────────────────────────────┐    │
│  │  - NFT Minting UI                                   │    │
│  │  - Document Upload (IPFS)                          │    │
│  │  - AI Execution Trigger                            │    │
│  │  - On-chain Receipt Viewer                         │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
          │                                    │
          │ HTTP/REST                          │ Web3
          │                                    │
┌─────────▼────────────────────┐    ┌─────────▼──────────────┐
│   AI BACKEND (Python)        │    │   SOMNIA L1 (EVM)      │
│   ┌────────────────────┐    │    │   ┌────────────────┐   │
│   │  FastAPI Server    │    │    │   │  AccessNFT     │   │
│   │  - NFT verification│    │    │   │  (ERC-721)     │   │
│   │  - AI execution    │◄───┼────┼───┤  - mint()      │   │
│   │  - Trace logging   │    │    │   │  - ownerOf()   │   │
│   └────────┬───────────┘    │    │   └────────────────┘   │
│            │                 │    │   ┌────────────────┐   │
│   ┌────────▼───────────┐    │    │   │ AgentRegistry  │   │
│   │ Verifiable Agent   │    │    │   │ - registerAgent│   │
│   │ - DID identity     │    │    │   │ - isActive()   │   │
│   │ - inputRoot        │◄───┼────┼───┤                │   │
│   │ - executionRoot    │    │    │   └────────────────┘   │
│   │ - Merkle trees     │    │    │   ┌────────────────┐   │
│   └────────┬───────────┘    │    │   │  Provenance    │   │
│            │                 │    │   │ - record()     │   │
│            │ Store           │    │   │ - verify()     │   │
│            ▼                 │    │   └────────────────┘   │
│   ┌────────────────────┐    │    └────────────────────────┘
│   │  IPFS Client       │    │
│   │  (Pinata)          │    │
│   │  - upload docs     │    │
│   │  - upload traces   │    │
│   │  - fetch by CID    │    │
│   └────────────────────┘    │
└──────────────────────────────┘
```

---

## Data Flow (Execution Sequence)

```
┌─────────┐                                              ┌──────────┐
│  USER   │                                              │ SOMNIA   │
└────┬────┘                                              │   L1     │
     │                                                    └────┬─────┘
     │ 1. Login with email                                    │
     │────────────────────────────►                           │
     │         Crossmint                                      │
     │                                                        │
     │ 2. Mint Access NFT                                    │
     │───────────────────────────────────────────────────────►│
     │                                                        │
     │ 3. Upload document to IPFS                            │
     │────────────────────────────►                           │
     │         Pinata                                         │
     │         returns CID                                    │
     │◄───────────────────────────                            │
     │                                                        │
     │ 4. Update NFT with document CID                       │
     │───────────────────────────────────────────────────────►│
     │                                                        │
     │ 5. Request AI execution                               │
     │────────────────────────────►                           │
     │         Backend                                        │
     │                  ▼                                     │
     │           Check NFT ownership ────────────────────────►│
     │                  ▼                                     │
     │           Fetch doc from IPFS                         │
     │                  ▼                                     │
     │           Compute inputRoot                           │
     │                  ▼                                     │
     │           Execute LLM                                 │
     │           (log every step)                            │
     │                  ▼                                     │
     │           Build Merkle tree                           │
     │                  ▼                                     │
     │           Compute executionRoot                       │
     │                  ▼                                     │
     │           Upload trace to IPFS                        │
     │                  ▼                                     │
     │           Record on Somnia ───────────────────────────►│
     │                                                        │
     │ 6. Return receipt                                     │
     │◄───────────────────────────                            │
     │   (tx_hash, record_id, CIDs)                          │
     │                                                        │
     │ 7. View on Somnia Explorer                            │
     │───────────────────────────────────────────────────────►│
     │                                                        │
```

---

## Smart Contract Relationships

```
┌───────────────────────────────────────────────────────────┐
│                        SOMNIA L1                           │
│                                                            │
│  ┌──────────────────────────────────────────────────┐    │
│  │             AccessNFT (ERC-721)                   │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  State:                                 │      │    │
│  │  │  - tokenId → owner                      │      │    │
│  │  │  - tokenId → documentCID (tokenURI)     │      │    │
│  │  │  - tokenId → isLocked                   │      │    │
│  │  │  - agentRegistry (address)              │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  Functions:                             │      │    │
│  │  │  - mint(to, documentCID)                │      │    │
│  │  │  - hasAccess(user, tokenId)             │      │    │
│  │  │  - lockToken(tokenId)                   │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  └──────────────────────────────────────────────────┘    │
│                           │                                │
│                           │ references                     │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────┐    │
│  │            AgentRegistry                          │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  State:                                 │      │    │
│  │  │  - didHash → AgentClaim                 │      │    │
│  │  │     (controller, metadataURI, active)   │      │    │
│  │  │  - controller → didHash[]               │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  Functions:                             │      │    │
│  │  │  - registerAgent(did, name, metadata)   │      │    │
│  │  │  - isActiveAgent(did)                   │      │    │
│  │  │  - recordExecution(did)                 │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  └──────────────────────────────────────────────────┘    │
│                           │                                │
│                           │ both referenced by             │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────┐    │
│  │              Provenance                           │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  State:                                 │      │    │
│  │  │  - records[]                            │      │    │
│  │  │     (nftTokenId, inputCID, inputRoot,   │      │    │
│  │  │      outputCID, executionRoot, trace,   │      │    │
│  │  │      agentDID, timestamp)               │      │    │
│  │  │  - nftTokenId → recordIds[]             │      │    │
│  │  │  - agentDID → recordIds[]               │      │    │
│  │  │  - executionRoot → recordId             │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  │  ┌────────────────────────────────────────┐      │    │
│  │  │  Functions:                             │      │    │
│  │  │  - recordDerivative(...)                │      │    │
│  │  │  - getRecordsByNFT(tokenId)             │      │    │
│  │  │  - getRecord(recordId)                  │      │    │
│  │  │  - verifyProof(recordId, proof)         │      │    │
│  │  └────────────────────────────────────────┘      │    │
│  └──────────────────────────────────────────────────┘    │
│                                                            │
└───────────────────────────────────────────────────────────┘
```

---

## Verifiable Execution Flow

```
┌──────────────────────────────────────────────────────────┐
│              VERIFIABLE AGENT EXECUTION                   │
└──────────────────────────────────────────────────────────┘

INPUTS:
┌────────────────────────────┐
│ Document (IPFS CID)        │
│ + User Prompt              │
│ + Metadata                 │
└────────┬───────────────────┘
         │
         ▼
┌────────────────────────────┐
│  INPUT COMMITMENT          │
│  inputRoot = keccak256({   │
│    cid: "QmXYZ...",         │
│    chunks: [...],           │
│    metadata: {...},         │
│    timestamp: 1234567890    │
│  })                         │
└────────┬───────────────────┘
         │
         │ Anchor on Somnia
         ▼
┌────────────────────────────┐
│  Provenance.recordDerivative│
│  (inputRoot)                │
└────────────────────────────┘

EXECUTION:
┌────────────────────────────┐
│  Step 1: Prompt            │──┐
│  hash_1 = keccak256(...)   │  │
└────────────────────────────┘  │
                                 │
┌────────────────────────────┐  │
│  Step 2: LLM Call          │──┤ Build
│  hash_2 = keccak256(...)   │  │ Merkle
└────────────────────────────┘  │ Tree
                                 │
┌────────────────────────────┐  │
│  Step 3: Tool Call         │──┤
│  hash_3 = keccak256(...)   │  │
└────────────────────────────┘  │
                                 │
┌────────────────────────────┐  │
│  Step 4: Final Response    │──┘
│  hash_4 = keccak256(...)   │
└────────────────────────────┘
         │
         ▼
┌────────────────────────────┐
│  MERKLE TREE               │
│       executionRoot        │
│           /    \           │
│          /      \          │
│        h12      h34        │
│       /  \      /  \       │
│      h1  h2    h3  h4      │
└────────┬───────────────────┘
         │
         │ Upload to IPFS
         ▼
┌────────────────────────────┐
│  Trace JSON               │
│  CID: QmABC...            │
│  {                         │
│    steps: [...],           │
│    executionRoot: "0x...", │
│    timestamp: 1234567890   │
│  }                         │
└────────┬───────────────────┘
         │
         │ Anchor on Somnia
         ▼
┌────────────────────────────┐
│  Provenance.recordDerivative│
│  (executionRoot, traceCID)  │
└────────────────────────────┘

VERIFICATION:
┌────────────────────────────┐
│  Anyone can verify:        │
│  1. Fetch trace from IPFS  │
│  2. Recompute Merkle root  │
│  3. Compare with on-chain  │
│     executionRoot          │
│  ✅ Proof of execution      │
└────────────────────────────┘
```

---

## Crossmint Integration Flow

```
┌─────────────────────────────────────────────────────────┐
│                 CROSSMINT WALLET FLOW                    │
└─────────────────────────────────────────────────────────┘

USER:                    CROSSMINT:              BACKEND:
  │                          │                        │
  │ 1. Click "Login"         │                        │
  │─────────────────────────►│                        │
  │                          │                        │
  │ 2. Enter email           │                        │
  │─────────────────────────►│                        │
  │                          │                        │
  │ 3. Verify OTP            │                        │
  │─────────────────────────►│                        │
  │                          │                        │
  │ 4. Wallet created        │                        │
  │◄─────────────────────────│                        │
  │   address: 0xABC...      │                        │
  │                          │                        │
  │ 5. Request NFT mint      │                        │
  │──────────────────────────┼───────────────────────►│
  │   { address: 0xABC... }  │                        │
  │                          │                        │
  │                          │  6. Backend mints NFT  │
  │                          │     to user's address  │
  │                          │◄───────────────────────│
  │                          │                        │
  │                          │  7. Transaction on     │
  │                          │     Somnia L1          │
  │                          │                 ┌──────▼────┐
  │                          │                 │  Somnia   │
  │                          │                 │  AccessNFT│
  │                          │                 │  .mint()  │
  │                          │                 └──────┬────┘
  │                          │                        │
  │ 8. NFT now in wallet     │◄───────────────────────│
  │◄─────────────────────────│                        │
  │   (visible in Crossmint) │                        │
  │                          │                        │
  │ 9. User can now access   │                        │
  │    documents via NFT     │                        │
  │    ownership             │                        │
  │                          │                        │
```

**Key Points:**
- User NEVER sees private keys
- Crossmint provisions the wallet (0xABC...)
- Backend mints NFT to that address on Somnia
- On-chain, it looks like a normal Somnia NFT mint
- User interacts via Crossmint UI (email login)

---

## File Storage & Retrieval

```
UPLOAD:
┌──────────┐
│   User   │
└────┬─────┘
     │ 1. Select file
     ▼
┌──────────────────┐
│   Frontend       │
│ - Encrypt (opt)  │
│ - Compress (opt) │
└────┬─────────────┘
     │ 2. Upload to IPFS
     ▼
┌──────────────────┐
│   Pinata API     │
│ POST /pinning/   │
│ pinFileToIPFS    │
└────┬─────────────┘
     │ 3. Returns CID
     │   (QmXYZ...)
     ▼
┌──────────────────┐
│   Somnia Chain   │
│ AccessNFT        │
│ .setTokenURI(    │
│   tokenId, CID)  │
└──────────────────┘

RETRIEVAL:
┌──────────┐
│  Agent   │
└────┬─────┘
     │ 1. Check NFT ownership
     ▼
┌──────────────────┐
│   Somnia Chain   │
│ AccessNFT        │
│ .ownerOf(tokenId)│
│ .tokenURI(...)   │
└────┬─────────────┘
     │ 2. Returns CID
     ▼
┌──────────────────┐
│   IPFS Gateway   │
│ GET /ipfs/QmXYZ  │
└────┬─────────────┘
     │ 3. Returns file
     ▼
┌──────────────────┐
│   Agent          │
│ - Decrypt (opt)  │
│ - Process        │
│ - Log steps      │
└──────────────────┘
```

---

**These diagrams provide visual reference for the complete system architecture.**
